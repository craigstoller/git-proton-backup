# Task 3 record — v0.7.0 RELEASE GATE (draft artifacts)

Runner log, appended as work proceeds. Plan: `docs/superpowers/plans/2026-08-13-cli-0.8.0-certification.md`
(Task 3 block). Checklist by reference: `docs/research/gates/brief-checklist.md`.
Task 2 context: `task2-record.md` (same dir).
Timestamps: `Get-Date -Format o` (Pacific, DST -> UTC-07:00).

Scope: gate the BUILT RELEASE ARTIFACTS of draft release v0.7.0. Publishing is Craig's.
Confinement: writes ONLY under `/my-files/GitRemotes/rel070-gate`, plus creating
`/my-files/GitRemotes` if absent and trashing it at cleanup. Untouchables read-only.
Never `GPB_LIVE_ACCOUNT`, never `GPB_UNCERTIFIED_CLI`. Nothing pushed to git; no code changes.
BLOCKED-never-patch.

---

## Step 0 — Tooling probe

### 2026-08-13T21:04:58.1812217-07:00

```
gh: C:\Program Files\GitHub CLI\gh.exe
git: C:\Program Files\Git\cmd\git.exe
proton-drive: C:\Users\craig\Tools\proton-drive.exe
git-remote-proton (-All, PRE-install): C:\Users\craig\AppData\Local\Programs\git-proton-backup\git-remote-proton.exe
```

Exactly ONE `git-remote-proton` on PATH before this gate's install (no shadowing at entry).
`C:\gpb-cert` exists with Task 2's leavings (`clone1`, `helper`, `readback-after`,
`readback-before`, `smoke`, `junk-a`) — this gate uses fresh `rel070-*` subdirs and does not
touch those.

---

## Step 1 — Baseline

### 2026-08-13T21:05:19.0510180-07:00 — machine CLI `--version`

```
Proton Drive CLI cli-drive@0.8.0+06e8c605
Proton Drive SDK js@0.21.0+06e8c605
You are running the latest version.
version exit=0
```

**PASS** — expected `cli-drive@0.8.0+06e8c605` (Task 2's certified swap still in place).

### 2026-08-13T21:05:19-07:00 — credential metadata WATCH BASELINE (`cmdkey /list` filtered, no secret read)

```
      Local machine persistence

>     Target: LegacyGeneric:target=ch.proton.drive/drive-sdk-cli/auth-session
      Type: Generic
      User: auth-session
```

Identical to Task 1's and Task 2's baselines (target / type / user).

### 2026-08-13T21:05:19-07:00 — `ProjectsBackupDaily` state (READ-ONLY `schtasks /query`)

```
TaskName:                             \ProjectsBackupDaily
Next Run Time:                        N/A
Status:                               Disabled
Last Run Time:                        8/13/2026 12:30:01 PM
Last Result:                          1
Scheduled Task State:                 Disabled
Schedule Type:                        Daily
Start Time:                           12:30:00 PM
```

Recorded only. Still Disabled from Task 2 — **re-enabling remains Craig's step.** No `/change`
was issued by this runner.

### 2026-08-13T21:05:27.1465502-07:00 — `/my-files` BASELINE row set (authenticated, read-only)

`proton-drive filesystem list /my-files --json` → exit 0, 5 rows (uid | name | type | parentUid | trashTime):

```
tU-Ot1Sq63NwBcxlnl7IcA~5QY-B6t9Eui6VKsXvJPmuA  Project Repo Bundles        folder  tU-Ot1Sq63NwBcxlnl7IcA~iMF_ohkUGz7d0J77g_Lb4g  (none)
tU-Ot1Sq63NwBcxlnl7IcA~n94X-kyGohebE_FBtxA5Sg  Sensitive Project Sources   folder  tU-Ot1Sq63NwBcxlnl7IcA~iMF_ohkUGz7d0J77g_Lb4g  (none)
tU-Ot1Sq63NwBcxlnl7IcA~TfvH3TrgNW1Eyo6Urs7hmQ  _cas-probe                  folder  tU-Ot1Sq63NwBcxlnl7IcA~iMF_ohkUGz7d0J77g_Lb4g  (none)
tU-Ot1Sq63NwBcxlnl7IcA~ThDi8B_92zkL76UXhjqFng  ChatGPT Export Text Backup  folder  tU-Ot1Sq63NwBcxlnl7IcA~iMF_ohkUGz7d0J77g_Lb4g  (none)
tU-Ot1Sq63NwBcxlnl7IcA~Vho9hzKVaqnBLf7UnwC64w  GitBackups                  folder  tU-Ot1Sq63NwBcxlnl7IcA~iMF_ohkUGz7d0J77g_Lb4g  (none)
```

(The `--json` `name` field is a `{ok,value}` wrapper; the `value` is shown above.) No `trashTime`
on any row. **Row-set EQUAL to Task 2's entry baseline and its post-cleanup final state** — the
account is at the precondition baseline. `/my-files/GitRemotes` is **ABSENT** (not among the 5
rows), as Task 2's cleanup left it; parent creation via the two-argument form is authorised.

---

## Step 2 — version.json drift gate (before anything else)

### 2026-08-13T21:05:40.9750419-07:00 — feed fetch

`https://proton.me/download/drive/cli/version.json` → **StatusCode 200** (reachable).

```
Releases count: 1
Releases[0]: CategoryName=Stable Version=0.8.0 ReleaseDate=2026-08-13
Files count: 9
  Platform=macos/arm64
  Platform=macos/x64
  Platform=linux/arm64
  Platform=linux/x64
  Platform=linux/x64-baseline
  Platform=linux/arm64-musl
  Platform=linux/x64-musl
  Platform=windows/arm64
  Platform=windows/x64
--- windows/x64 entry ---
Url: https://proton.me/download/drive/cli/0.8.0/windows-x64/proton-drive.exe
Sha512CheckSum: 03ce039618212c4cb9f175e3d53889657484cee9bfb0b1a24ad3f5b5e703ccf5784119acfa44c65bdda2af2b273c11b91075424ac169824b28edac49399b5a9a
```

Feed well-formed; all 9 platform entries present; **windows/x64 present** → the fail-closed
condition (unreachable / malformed / missing windows-x64) is **NOT** triggered.

### 2026-08-13T21:05:46.2120548-07:00 — installed binary recomputation

```
installed proton-drive.exe SHA-512: 03ce039618212c4cb9f175e3d53889657484cee9bfb0b1a24ad3f5b5e703ccf5784119acfa44c65bdda2af2b273c11b91075424ac169824b28edac49399b5a9a
size: 121838592
MATCH REF: True
```

Three-way equality — feed windows/x64 SHA-512 == Task 2 drift reference == freshly recomputed
digest of `C:\Users\craig\Tools\proton-drive.exe`. **NO DRIFT. Drift gate PASS.**

---

## Step 3 — Draft staging

### 2026-08-13T21:06:25.9031081-07:00 — download into a fresh empty dir

`C:\gpb-cert\rel070-stage` created fresh (did not pre-exist).

```
gh release download v0.7.0 --repo craigstoller/git-proton-backup --dir C:\gpb-cert\rel070-stage
download exit=0

git-remote-proton.exe           3983872 bytes
git-remote-proton.exe.sha256         87 bytes
install.ps1                        8805 bytes
```

Three assets, sizes exactly as the brief states (3983872 / 87 / 8805).

### 2026-08-13T21:06:35.5449720-07:00 — STAGED BASELINE (SHA-256; the publish closure compares against these)

| Asset | Size (B) | SHA-256 |
|---|---|---|
| `git-remote-proton.exe` | 3983872 | `00f70579dd2d168ebbd1d24a8235058c2fcaf66bbc63c0e7b3c75c9737e04447` |
| `git-remote-proton.exe.sha256` | 87 | `8156e5f529058d093f8382679f0a887dd94cfbb6d5239dffe6580575784a8391` |
| `install.ps1` | 8805 | `8987ab2b73e66a79b5a33c4652d92ca10d2a2aae3fe4a2e1174b3519858575e0` |

### 2026-08-13T21:06:35-07:00 — independent sidecar verification

Sidecar content, verbatim (87 bytes, two-space `sha256sum` form):

```
00f70579dd2d168ebbd1d24a8235058c2fcaf66bbc63c0e7b3c75c9737e04447  git-remote-proton.exe
```

```
declared: 00f70579dd2d168ebbd1d24a8235058c2fcaf66bbc63c0e7b3c75c9737e04447
actual:   00f70579dd2d168ebbd1d24a8235058c2fcaf66bbc63c0e7b3c75c9737e04447
SIDECAR MATCH: True
```

**PASS** — the sidecar's declared digest equals the exe's independently recomputed SHA-256.

### 2026-08-13T21:06:43.3729226-07:00 — draft metadata (corroboration)

`gh release view v0.7.0 --json …` → `"isDraft": true`, `"publishedAt": null`,
`"targetCommitish": "main"`, `createdAt` `2026-08-14T03:48:19Z`, assets `createdAt`
`2026-08-14T04:02:42Z`, all three `"state": "uploaded"`, `downloadCount` 0.
GitHub's own API-reported asset digests:

```
git-remote-proton.exe         sha256:00f70579dd2d168ebbd1d24a8235058c2fcaf66bbc63c0e7b3c75c9737e04447
git-remote-proton.exe.sha256  sha256:8156e5f529058d093f8382679f0a887dd94cfbb6d5239dffe6580575784a8391
install.ps1                   sha256:8987ab2b73e66a79b5a33c4652d92ca10d2a2aae3fe4a2e1174b3519858575e0
```

**All three equal the locally recomputed staged baseline** — the download is byte-faithful to
what the Release workflow uploaded. (Asset URLs still carry the `untagged-fadef3c2b3f0f90b8470`
draft path, as expected for an unpublished draft.)

Staged `install.ps1` is **byte-identical** to the worktree's `install.ps1`
(`8987ab2b…858575e0` both) — the workflow shipped the reviewed script unmodified.

---

## Step 4 — Install the DRAFT helper (real run)

### 2026-08-13T21:07:01.9465065-07:00 — PRE-install state

```
installed helper: C:\Users\craig\AppData\Local\Programs\git-proton-backup\git-remote-proton.exe
  size: 2507776
  sha256: 46e0e3e62db83c5d90730de18494c2f3a936b92ab6bb4f7e9eec4c8152477d96
  --version: git-remote-proton: must be run by git as a remote helper   (exit 1)
```

The pre-existing install is the **older released helper** (2507776 B; it has no `--version`
flag at all, so it prints the run-by-git message and exits 1). This makes step 5's `--version`
assertion genuinely discriminating: the expected string can only come from the newly installed
draft bytes.

User PATH already contains `…\Programs\git-proton-backup` (value kind `String`), so the
installer's PATH-append branch is a no-op this run and prints nothing — expected, not a defect.

### 2026-08-13T21:07:11.3458751-07:00 — `& 'C:\gpb-cert\rel070-stage\install.ps1'` (no `-SkipPathUpdate`)

Verbatim output, complete (all streams merged):

```
Helper installed: C:\Users\craig\AppData\Local\Programs\git-proton-backup\git-remote-proton.exe
No GitProtonBackup module payload beside the script — helper-only install. For the module, clone the repository and re-run.
```

- **No checksum throw** — the script's sidecar gate passed against the staged pair.
- **No shadow warning** — the PATH walk found the just-installed copy first.
- **Helper-only install message present and CORRECT**: a release download is exe + sha256 +
  script with no module payload beside it; the script is written (peer-review blocker, per its
  own comment at lines 126-129) to install the helper and SKIP the module rather than throw.
  This is the expected message for this gate's install step, not a warning.
- No error, no exception; the script ran under `$ErrorActionPreference = 'Stop'`.

---

## Step 5 — Fresh-shell checks

### 2026-08-13T21:07:24.7526274-07:00

Installed bytes equal the staged asset:

```
size:   3983872
sha256: 00f70579dd2d168ebbd1d24a8235058c2fcaf66bbc63c0e7b3c75c9737e04447
== STAGED exe digest: True
```

PATH resolution (each PowerShell tool invocation is a NEW process, i.e. a fresh shell):

```
(Get-Command git-remote-proton -All).Source
count: 1
  C:\Users\craig\AppData\Local\Programs\git-proton-backup\git-remote-proton.exe
FIRST HIT == installed helper: True
```

**PASS** — exactly one `git-remote-proton` on PATH, and it is the installed draft helper. No
shadowing.

Version string, verbatim:

```
git-remote-proton v0.7.0 (certified CLI: cli-drive@0.8.0+06e8c605)
version exit=0
```

**PASS** — byte-for-byte the expected line
`git-remote-proton v0.7.0 (certified CLI: cli-drive@0.8.0+06e8c605)`.

---

## Step 6 — Smoke: INSTALLED DRAFT HELPER against the machine's certified 0.8.0 CLI

Every git command below resolves `git-remote-proton` to the just-installed release bytes
(`00f70579…`, the only copy on PATH), and every `proton-drive` call is the swapped 0.8.0.

### 2026-08-13T21:08:11.8144324-07:00 — create the authorised parent

```
proton-drive filesystem create-folder /my-files GitRemotes   (two-arg form)
create exit=0  -> uid tU-Ot1Sq63NwBcxlnl7IcA~sTqkfe5d3DGUzAmWn-XO5w
                 parentUid tU-Ot1Sq63NwBcxlnl7IcA~iMF_ohkUGz7d0J77g_Lb4g  type folder
info exit=0 (confirms the same uid)
```

### 2026-08-13T21:08:26.7605050-07:00 — local repo

```
git init -b main C:\gpb-cert\rel070-smoke                    exit 0
git commit --allow-empty -m "rel070 gate: bootstrap"         exit 0   C1 = e6c6c3ff8ef064c1a51ddfeacad6f4deee0f66ad
git remote add proton-v2 "proton::/my-files/GitRemotes/rel070-gate"   exit 0
```

### 2026-08-13T21:08:31 → 21:11:21-07:00 — bootstrap push (`git push -u proton-v2 main`)

```
gpb: downloaded /my-files/GitRemotes/rel070-gate/.lock (115 bytes)
gpb: downloaded /my-files/GitRemotes/rel070-gate/refs/heads/main (41 bytes)
gpb: downloaded /my-files/GitRemotes/rel070-gate/HEAD (21 bytes)
To proton::/my-files/GitRemotes/rel070-gate
 * [new branch]      main -> main
gpb: downloaded /my-files/GitRemotes/rel070-gate/.lock (115 bytes)
branch 'main' set up to track 'proton-v2/main'.
push exit=0
```

(~2m50s; run under the 600000 ms tool timeout per checklist rule 9.)

### 2026-08-13T21:11:27 → 21:14:06-07:00 — update push (the ref update; `create-new-revision`)

Second empty commit `C2 = 76c19fb96aa5194c1aeacd5111e6b13d0c46c1d9`.

```
To proton::/my-files/GitRemotes/rel070-gate
   e6c6c3f..76c19fb  main -> main
push exit=0
```

`refs/heads/main` node, BEFORE vs AFTER:

| | BEFORE (21:11:27) | AFTER (21:14:06) |
|---|---|---|
| node uid | `…~QZkSL7C8KXsAekRdtw-Hjg` | `…~QZkSL7C8KXsAekRdtw-Hjg` (**identical**) |
| activeRevision uid | `…~MjJRxwp53yWuQw4X5uClBQ` | `…~3iB2W5E2cuMSgKRFjMZ86g` (**changed once**) |
| totalStorageSize | 119 | 238 (both revisions retained; active `storageSize` still 119) |
| claimedSize | 41 | 41 |
| `refs/heads` rows | 1 (`main`) | 1 (`main`) — no duplicate, no renamed sibling |

**PASS** — the release bytes drive `create-new-revision` with stable node identity, exactly as
Task 2 certified for the dev build.

### 2026-08-13T21:14:21 → 21:15:30-07:00 — clone

`git clone -o proton-v2 proton::/my-files/GitRemotes/rel070-gate C:\gpb-cert\rel070-clone` →
**exit 0** (2 packs + 2 idx downloaded).

```
source HEAD:          76c19fb96aa5194c1aeacd5111e6b13d0c46c1d9
clone HEAD:           76c19fb96aa5194c1aeacd5111e6b13d0c46c1d9
clone proton-v2/main: 76c19fb96aa5194c1aeacd5111e6b13d0c46c1d9
MATCH: True

git log --oneline (clone):
76c19fb rel070 gate: update
e6c6c3f rel070 gate: bootstrap
```

**rev-parse match: PASS**, and both commits are present in the clone.

Clean pre-junk `git ls-remote proton::/my-files/GitRemotes/rel070-gate` → **exit 0**:

```
76c19fb96aa5194c1aeacd5111e6b13d0c46c1d9	refs/heads/main
76c19fb96aa5194c1aeacd5111e6b13d0c46c1d9	HEAD
```

### 2026-08-13T21:16:22-07:00 — strict-fetch junk check

Fixture staged OUTSIDE the repo at `C:\gpb-cert\rel070-junk\junk-r`: 41 bytes, ASCII `'x'`×41.
Uploaded to `.../rel070-gate/refs/heads`:

```
Transfer summary:
  Uploaded: 1 items (41 B)
upload exit=0
```

It landed as `refs/heads/junk-r`, uid `tU-Ot1Sq63NwBcxlnl7IcA~YgBDi3BL0uMWkvGjoJH2hw`
(`refs/heads` then listed 2 rows: `main`, `junk-r`).

`git ls-remote proton::/my-files/GitRemotes/rel070-gate`, verbatim:

```
gpb: downloaded /my-files/GitRemotes/rel070-gate/gpb-remote.json (42 bytes)
gpb: downloaded /my-files/GitRemotes/rel070-gate/refs/heads/main (41 bytes)
gpb: downloaded /my-files/GitRemotes/rel070-gate/refs/heads/junk-r (41 bytes)
git-remote-proton: skipping /my-files/GitRemotes/rel070-gate/refs/heads/junk-r: not a ref: "xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx"
git-remote-proton: cannot serve a fetch: 1 file(s) under refs/ are not valid refs and a restore would silently lack them:
  /my-files/GitRemotes/rel070-gate/refs/heads/junk-r: not a ref: "xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx"
delete these files first (proton-drive filesystem trash <path>, or the web UI; Proton trash keeps them restorable), then retry
ls-remote exit=128
```

**PASS** — nonzero, **exit 128** (the precedented code), and the enumerated error names the
offending file with its full remote path, the `skipNote` line, the count, the `%q`-escaped
41-byte preview, and the remediation sentence. The junk was downloaded (not size-skipped), so
the 41-byte fixture exercised the in-band classification path as designed.

### 2026-08-13T21:17:12 → 21:17:32-07:00 — verify-before-trash, trash, recovery

Verify-before-trash on `junk-r`: `filesystem info` confirmed uid
`tU-Ot1Sq63NwBcxlnl7IcA~YgBDi3BL0uMWkvGjoJH2hw`, name `junk-r`, **type `file`** (no subtree
beneath it), `claimedSize` 41, `creationTime` `2026-08-14T04:16:24Z` — this run's own fixture,
matching the uid recorded at upload. Trash → `✅ junk-r`, exit 0. `refs/heads` then listed
**1 live row** (`main`), no `trashTime` on it — no trash lag, the ~30 s tolerance was not needed.

`git ls-remote` **green again**, exit 0, same two lines as before the junk:

```
76c19fb96aa5194c1aeacd5111e6b13d0c46c1d9	refs/heads/main
76c19fb96aa5194c1aeacd5111e6b13d0c46c1d9	HEAD
```

Credential metadata re-checked at 21:17:32 (after the whole authenticated stretch): target /
type / user **unchanged** from the step-1 baseline. **No STOP.**

**Smoke: PASS.**

---

## Step 7 — Cleanup

### 2026-08-13T21:18:49.8554099-07:00 — verify-before-trash: FULL subtree enumeration

`/my-files/GitRemotes`, recursively (checklist rule 3):

```
  rel070-gate                                          [folder] uid=…~poAEZrTSomzl1iJ2QqSs0Q
    gpb-remote.json                                    [file]   uid=…~_YJloi4ebnoAKiDfXaGK-g size=120
    HEAD                                               [file]   uid=…~ocrb7s70qa54OiSWyr8Ovg size=99
    packs                                              [folder] uid=…~HA9DGNCYAvESdt-g5X-B5A
      pack-42a29dd6….idx                               [file]   uid=…~B0k_42cwHZkIDb07OWmApg size=1208
      pack-42a29dd6….pack                              [file]   uid=…~X_qpGZ7jeuDAjYLRPPwDlA size=273
      pack-759cb27f….idx                               [file]   uid=…~blx6OZg4jv81MBQqDr5Pzw size=1180
      pack-759cb27f….pack                              [file]   uid=…~han4_oR6tlZZNbag6uRXkA size=294
    refs                                               [folder] uid=…~4pCIdbyccXte9mhVxNaBpQ
      heads                                            [folder] uid=…~wmUXZp7tAvYEvd6zvnMV8g
        main                                           [file]   uid=…~QZkSL7C8KXsAekRdtw-Hjg size=238
      tags                                             [folder] uid=…~odrC5qfCYyRoUoHN3yiRXw   (empty)
```

12 live nodes, **every one this gate's own artifact**; no foreign content; no untouchable folder
anywhere in the subtree or in any trash argument; no `trashTime` on any row; no orphaned `.lock`
(both pushes released it).

### 2026-08-13T21:19:19.7051808-07:00 — trash `rel070-gate`, confirm the parent is empty

```
trash /my-files/GitRemotes/rel070-gate -> ✅ rel070-gate    exit 0
list  /my-files/GitRemotes             -> live row count: 0    exit 0
```

Empty on the FIRST listing — **no trash lag**, the ~30 s re-list tolerance was not needed.

### 2026-08-13T21:19:33.1021011-07:00 — verify-before-trash + trash `GitRemotes`

`filesystem info /my-files/GitRemotes` re-confirmed uid
`tU-Ot1Sq63NwBcxlnl7IcA~sTqkfe5d3DGUzAmWn-XO5w` (the folder this run created at 21:08:15), type
`folder`, and the listing above showed it empty. Trash → `✅ GitRemotes`, exit 0.

### 2026-08-13T21:19:57.0973883-07:00 — FINAL `/my-files` row-set comparison

```
baseline count: 5 ; final count: 5
MISSING from final: NONE
EXTRA in final:     NONE
ROW-SET EQUAL: True
```

Compared field-by-field on `uid|name|type|parentUid` (checklist rule 1), never on serialised
JSON. No `trashTime` on any surviving row. All four untouchables plus `_cas-probe` present and
unmodified. **Account restored to the precondition baseline.**

### Credential watch

`cmdkey /list` (filtered, secret never read) at **21:05:19** (baseline), **21:17:32** (after the
authenticated stretch) and **21:19:57** (final): target
`LegacyGeneric:target=ch.proton.drive/drive-sdk-cli/auth-session`, Type `Generic`, User
`auth-session` — **identical at every watch point. No change, no STOP.**

### Trash tally

Runner-issued trash operations: **3** — `junk-r` (during the smoke), `rel070-gate`, `GitRemotes`.

Live nodes removed from the account tree by those operations: **14** — **6 folders**
(`rel070-gate`, `packs`, `refs`, `refs/heads`, `refs/tags`, `GitRemotes`) and **8 files**
(`gpb-remote.json`, `HEAD`, 4 pack/idx files, `refs/heads/main`, `junk-r`). Folders are counted,
per checklist rule 7. Everything trashed is in Proton's trash and restorable; the trash was
**not** emptied by the runner.

---

## Step 8 — Final drift re-check

### 2026-08-13T21:20:13.8259210-07:00

```
StatusCode: 200
Releases[0]: CategoryName=Stable Version=0.8.0 ReleaseDate=2026-08-13
Files count: 9
Url: https://proton.me/download/drive/cli/0.8.0/windows-x64/proton-drive.exe
Sha512CheckSum: 03ce039618212c4cb9f175e3d53889657484cee9bfb0b1a24ad3f5b5e703ccf5784119acfa44c65bdda2af2b273c11b91075424ac169824b28edac49399b5a9a
MATCH Task2 ref: True
```

Machine CLI re-check:

```
Proton Drive CLI cli-drive@0.8.0+06e8c605
Proton Drive SDK js@0.21.0+06e8c605
You are running the latest version.
exit=0
installed SHA-512: 03ce039618212c4cb9f175e3d53889657484cee9bfb0b1a24ad3f5b5e703ccf5784119acfa44c65bdda2af2b273c11b91075424ac169824b28edac49399b5a9a
```

Helper re-check: `git-remote-proton v0.7.0 (certified CLI: cli-drive@0.8.0+06e8c605)`, exit 0.

**NO DRIFT at gate exit.** The plan's publish-time drift gate is Craig's moment; this is the
freshest state the STOP report can carry (21:20 PT). Staged assets re-hashed at 21:19:57 and
**unchanged** from the step-3 baseline.

---

## Final state and verdict

### Machine / repo disposition

- Machine CLI: `cli-drive@0.8.0+06e8c605`, SHA-512 `03ce0396…399b5a9a` — unchanged by this gate.
- Installed helper: the DRAFT v0.7.0 release bytes (`00f70579…`), the only `git-remote-proton`
  on PATH. This is the deploy order the plan prescribes (the Task 2.4 refusal window closed at
  the install step); the installed helper is the release candidate, not a dev build.
- `\ProjectsBackupDaily`: **still Disabled** — the runner did NOT re-enable it. **Craig's step.**
- `GPB_LIVE_ACCOUNT` / `GPB_UNCERTIFIED_CLI`: never set by this runner.
- Nothing pushed to git; no code changes. Worktree `worktree-cli-080-cert` HEAD `07e7d84`,
  `git status --porcelain` shows only `?? .superpowers/` (this untracked evidence dir). Main
  repo HEAD `5e22cf3`, clean.
- Local gate dirs left in place for inspection: `C:\gpb-cert\rel070-stage` (staged assets),
  `rel070-smoke`, `rel070-clone`, `rel070-junk`.

### Verdict

**RELEASE GATE PASS** — the DRAFT v0.7.0 artifacts are gated and ready for Craig's publish.
Provisional until the post-publication digest closure (three-way: staged baseline above vs the
published assets vs the workflow's build output), as in Stages 4–6.

STAGED BASELINE the publish closure must compare against:

```
git-remote-proton.exe          3983872 B  00f70579dd2d168ebbd1d24a8235058c2fcaf66bbc63c0e7b3c75c9737e04447
git-remote-proton.exe.sha256        87 B  8156e5f529058d093f8382679f0a887dd94cfbb6d5239dffe6580575784a8391
install.ps1                       8805 B  8987ab2b73e66a79b5a33c4652d92ca10d2a2aae3fe4a2e1174b3519858575e0
```

Publishing is Craig's. **STOP.**


---

# Publication digest closure — CLOSURE PASS (2026-08-13 ~21:35 PT, controller-run, read-only)

Craig published via the GitHub UI (publishedAt 2026-08-14T04:27:11Z). Asset ids
(RA_kwDOTfWuiM4eoYbL/-bN/-bP) and createdAt (04:02:42Z, the draft build time) carried through
publication unchanged — re-homed, no re-upload, same as every prior release. Three-way closure
against the staged baseline: (1) fresh download to C:\gpb-cert\rel070-closure — all three
SHA-256 EQUAL the staged baseline (exe 00f70579…, sidecar 8156e5f5…, install.ps1 8987ab2b…);
(2) staging-dir re-hash — all three EQUAL; (3) byte-for-byte staged vs published — all three
SequenceEqual True. ProjectsBackupDaily re-enabled by Craig (verified Ready).

**Verdict now FINAL: v0.7.0 RELEASE GATE — PASS. The certification of cli-drive@0.8.0+06e8c605
is complete end to end** (hermetic audit → 17/17 live contract rows → smoke with stable node
identity → machine swap + green fleet verify → release-artifact gate → digest closure).
